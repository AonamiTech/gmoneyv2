# Table Magic authority corpus orchestration

Date: 2026-09-07 UTC

Status: working inventory tooling complete; authority seal held

The authority CLI now has a separate intake boundary for the explicitly
non-authoritative `working152` inventory. Intake requires a content hash and explicit
eligibility, non-synthetic, and non-duplicate decisions for every PDF. It rejects
ambiguous decisions, duplicate bytes, unreadable PDFs, synthetic inputs, and sources
outside the supplied roots before storing any content-addressed object.

The orchestration layer also provides:

- deterministic source-hash assignment with `production14 ⊆ passing36 ⊆ staging159`;
- a partial-assignment mode whose status remains non-authoritative;
- four machine-blind Luna queues (independent A, independent B, adjudicator, and red team);
- queue identity validation against the immutable 300-DPI render manifest; and
- an exact `seal-readiness` report that records counts and blockers without emitting or
  claiming an authority seal.

The fixed authority contract remains exactly 14/36/159. Working-corpus counts and tests do not
alter that contract. Sensitive PDFs, renders, annotations, and generated queues stay outside Git.

## Remaining blocker

The current working set is short by seven PDFs before the `staging159` master can be complete,
and every selected document still requires a complete frozen four-pass review set. No fake gold,
synthetic authority record, or seal was generated by this change.
